---
description: 'A service meant to list a set of quotes coming from a text file'
labels: ['bff', 'shared-types', 'quotes']
---

# Listing quotes

This service takes care of reading a local file and returning the quotes following the structure of the `QuoteType` type coming from `@deleteman/quotes-lister.shared-types` module.

On every request, the list of quotes returned will be the same.

